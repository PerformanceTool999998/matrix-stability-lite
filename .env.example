# MatrixStability Lite 环境变量
# 复制为 .env 后修改

# 数据库（默认 SQLite，无需修改）
DATABASE_URL=sqlite:///./matrix_lite.db

# 服务端端口
PORT=8000

# Agent 配置（可选）
AGENT_DEVICE_ID=DEMO_DEVICE_001
AGENT_SERVER_URL=http://localhost:8000/api/v1